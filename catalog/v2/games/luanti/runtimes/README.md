# Luanti runtimes

No Luanti runtime is currently published.

The previous `luanti.stable` definition used the reserved `local` artifact provider, which current Linux/Windows Agents do not execute. The definition is preserved under `../deferred/` until a typed executable installation strategy is implemented.
