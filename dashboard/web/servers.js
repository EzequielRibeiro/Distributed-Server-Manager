(function(){"use strict";
const API="/api";const state={instances:[],filter:"all",query:"",sidebarCollapsed:false};const el=id=>document.getElementById(id);const auth=()=>sessionStorage.getItem("dsm_auth")||"";
async function request(path){const r=await fetch(path,{headers:{Authorization:"Basic "+auth(),Accept:"application/json"}});if(r.status===401){sessionStorage.clear();location.replace("login.html");throw new Error("Sessão expirada");}const p=await r.json().catch(()=>({}));if(!r.ok)throw new Error(p.error||`HTTP ${r.status}`);return p;}function applySidebarState(c){state.sidebarCollapsed=c;document.body.classList.toggle("cap-sidebar-collapsed",c);localStorage.setItem("cap_sidebar_collapsed",c?"1":"0")}
async function loadSidebar(){const host=el("sidebar-component");if(!host)return;const r=await fetch("components/sidebar-v3.html");if(!r.ok)throw new Error(`sidebar HTTP ${r.status}`);host.innerHTML=await r.text();host.querySelectorAll("nav a").forEach(a=>a.classList.toggle("active",a.getAttribute("href")==="servers.html"));el("btn-logout")?.addEventListener("click",()=>{sessionStorage.clear();location.replace("login.html")});const who=await request("/api/whoami");el("current-user").textContent=`${who.username} (${who.role})`;el("servers-user-name").textContent=who.username||"—";el("servers-user-role").textContent=who.role||"—";document.querySelectorAll(".admin-only").forEach(x=>x.style.display=who.role==="admin"?"":"none");document.querySelectorAll(".agent-manager-only").forEach(x=>x.style.display=["admin","controller"].includes(who.role)?"":"none");document.querySelectorAll(".instance-manager-only").forEach(x=>x.style.display=["admin","controller","operator"].includes(who.role)?"":"none");applySidebarState(localStorage.getItem("cap_sidebar_collapsed")==="1");el("servers-menu-toggle")?.addEventListener("click",()=>{if(window.innerWidth<=760){document.body.classList.toggle("sidebar-open");return}applySidebarState(!state.sidebarCollapsed)});host.querySelectorAll("a").forEach(a=>a.addEventListener("click",()=>document.body.classList.remove("sidebar-open")))}
function identity(i){return{server:i.server||i.node||i.node_id||"",game:i.game||"unknown",instance:i.instance||i.id||"unknown"}}function gameIcon(g){g=String(g).toLowerCase();if(g.includes("minecraft"))return"🧊";if(g.includes("dayz"))return"◈";if(g.includes("rust"))return"⬢";if(g.includes("arma"))return"◆";return"🎮"}function normalizeStatus(s){const r=String(s?.server_state?.status?.state||s?.status?.state||s?.state||s?.status||"unknown").toLowerCase();if(["online","running","started","active"].includes(r))return"online";if(["starting","updating","installing","warning","degraded"].includes(r))return"attention";return"offline"}function number(v,f=0){const n=Number(v);return Number.isFinite(n)?n:f}
function normalize(resource,summary,agents){const id=identity(resource),meta=summary?.instance_metadata||{},metrics=summary?.metrics||{},serverState=summary?.server_state||{},status=normalizeStatus(summary),players=number(serverState?.players??metrics?.players??summary?.players,0),slots=number(serverState?.max_players??serverState?.slots??summary?.max_players,0),ramMb=number(metrics?.instance?.memory_mb??metrics?.memory_mb??metrics?.ram_mb,0),cpu=number(metrics?.instance?.cpu_pct??metrics?.cpu_pct??metrics?.cpu?.pct,0),agentId=meta.agent_id||meta.agent||id.server,agent=agents.find(a=>[a.id,a.agent_id,a.node_id,a.name].filter(Boolean).map(String).includes(String(agentId))),location=agent?.location_name||agent?.location||agent?.datacenter_name||agent?.datacenter||meta.datacenter||"-";return{...id,status,players,slots,ramMb,cpu,display:meta.display_name||id.instance,agent:agent?.name||agent?.id||agent?.agent_id||agentId||"-",location}}
async function enrich(resources,agents){const result=new Array(resources.length);let cursor=0;async function worker(){while(cursor<resources.length){const i=cursor++,r=resources[i],id=identity(r);let summary=null;if(i<48&&id.server&&id.game&&id.instance){try{summary=await request(`/api/runtime?${new URLSearchParams(id)}`)}catch(_){}}result[i]=normalize(r,summary,agents)}}await Promise.all(Array.from({length:Math.min(6,resources.length||1)},worker));return result}function fmtRam(mb){return !mb?"-":mb>=1024?`${(mb/1024).toFixed(2)} GB`:`${Math.round(mb)} MB`}function escapeHtml(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}
function card(i){const a=document.createElement("article");a.className="cap-instance-card";a.dataset.state=i.status;const label=i.status==="online"?"Online":i.status==="attention"?"Atenção":"Offline",players=i.slots?`${i.players} / ${i.slots}`:String(i.players),ramPct=Math.max(0,Math.min(100,i.ramMb?Math.min(100,(i.ramMb/16384)*100):0)),qs=new URLSearchParams({server:i.server,game:i.game,instance:i.instance});a.innerHTML=`<div class="cap-instance-head"><div class="cap-instance-title"><span class="cap-game-mark">${gameIcon(i.game)}</span><div><strong>${escapeHtml(i.display)}</strong><small>${escapeHtml(i.game)} · ${escapeHtml(i.agent)}</small></div></div><span class="cap-state ${i.status}">${label}</span></div><div class="cap-instance-meta"><div><span>Jogadores</span><strong>${players}</strong></div><div><span>CPU</span><strong>${i.cpu?i.cpu.toFixed(1)+"%":"-"}</strong></div><div><span>Agent</span><strong>${escapeHtml(i.agent)}</strong></div><div><span>Localização</span><strong>${escapeHtml(i.location)}</strong></div></div><div class="cap-meter"><div class="cap-meter-row"><span>RAM</span><strong>${fmtRam(i.ramMb)}</strong></div><div class="cap-meter-track"><div class="cap-meter-fill" style="width:${ramPct}%"></div></div></div><div class="cap-instance-actions"><a class="cap-action-primary" href="console.html?${qs}">Console</a><a class="cap-action-secondary" href="catalog.html">Gerenciar</a></div>`;return a}
function render(){const q=state.query.trim().toLowerCase(),visible=state.instances.filter(i=>(state.filter==="all"||i.status===state.filter)&&(!q||[i.display,i.game,i.instance,i.agent,i.server,i.location].some(v=>String(v).toLowerCase().includes(q))));el("instance-grid").replaceChildren(...visible.map(card));el("instance-empty").hidden=visible.length>0;el("instance-summary-text").textContent=`${visible.length} de ${state.instances.length} instância(s) exibida(s).`;el("fleet-total").textContent=state.instances.length;el("fleet-online").textContent=state.instances.filter(i=>i.status==="online").length;el("fleet-offline").textContent=state.instances.filter(i=>i.status==="offline").length;el("fleet-agents").textContent=new Set(state.instances.map(i=>i.agent).filter(v=>v&&v!=="-")).size}
async function load(){const btn=el("refresh-instances");if(btn)btn.disabled=true;el("instance-summary-text").textContent="Sincronizando Runtime...";try{const[r,a]=await Promise.all([request("/api/runtime/list"),request("/api/agents").catch(()=>({agents:[]}))]);state.instances=await enrich(Array.isArray(r)?r:(r.resources||[]),Array.isArray(a)?a:(a.agents||[]));render()}catch(e){state.instances=[];render();el("instance-summary-text").textContent=`Não foi possível carregar as instâncias: ${e.message}`}finally{if(btn)btn.disabled=false}}function bind(){el("refresh-instances")?.addEventListener("click",load);el("instance-search")?.addEventListener("input",e=>{state.query=e.target.value;render()});document.querySelectorAll(".cap-filter").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".cap-filter").forEach(x=>x.classList.remove("active"));b.classList.add("active");state.filter=b.dataset.filter;render()}))}async function init(){if(!auth()){location.replace("login.html");return}try{await loadSidebar()}catch(e){console.error("Sidebar error",e)}bind();await load()}document.addEventListener("DOMContentLoaded",init)})();
