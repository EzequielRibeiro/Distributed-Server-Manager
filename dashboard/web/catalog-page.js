(async function(){'use strict';const auth=sessionStorage.getItem('dsm_auth')||'';if(!auth){location.href='/login.html';return;}async function req(path){const r=await fetch(path,{headers:{Authorization:'Basic '+auth,Accept:'application/json'}});if(r.status===401){sessionStorage.removeItem('dsm_auth');location.href='/login.html';throw new Error('unauthorized');}return r.json();}try{const sidebar=await fetch('/components/sidebar-v3.html').then(r=>r.text());document.getElementById('sidebar-component').innerHTML=sidebar;}catch(e){}try{const me=await req('/api/auth/me');const user=me.user||me;const role=String(user.role||'').toLowerCase();document.getElementById('catalog-user-name').textContent=user.username||user.name||'Sessão ativa';document.getElementById('catalog-user-role').textContent=role||'—';const admin=['admin','controller'].includes(role);const manager=admin||role==='operator';document.querySelectorAll('.admin-only').forEach(el=>el.hidden=!admin);document.querySelectorAll('.instance-manager-only').forEach(el=>el.hidden=!manager);document.querySelectorAll('.agent-manager-only').forEach(el=>el.hidden=!manager);}catch(e){}const sidebar=document.querySelector('.cap-sidebar-v3');document.getElementById('catalog-menu-toggle')?.addEventListener('click',()=>document.body.classList.toggle('cap-sidebar-collapsed'));document.getElementById('btn-logout')?.addEventListener('click',()=>{sessionStorage.removeItem('dsm_auth');location.href='/login.html';});document.querySelectorAll('.cap-sidebar-v3 a').forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='catalog.html'));})();