(function(){"use strict";const auth=()=>sessionStorage.getItem("dsm_auth")||"";const $=id=>document.getElementById(id);let snapshot={members:[],instances:[],capabilities:{}};async function request(path,options={}){const headers={Authorization:`Basic ${auth()}`,Accept:"application/json"};if(options.body)headers["Content-Type"]="application/json";const response=await fetch(path,{...options,headers});if(response.status===401){sessionStorage.removeItem("dsm_auth");sessionStorage.removeItem("dsm_customer_auth");location.href="/customer-login.html";throw new Error("Sessão encerrada.")}const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.error||`HTTP ${response.status}`);return data}async function mutate(path,body){const data=await request(path,{method:"POST",body:JSON.stringify(body)});if(data.members)renderTeam(data);return data}function option(value,label,current){const el=document.createElement("option");el.value=value;el.textContent=label;el.selected=current===value;return el}function show(error){$("customer-members-message").textContent=error.message||String(error)}function button(label,handler){const el=document.createElement("button");el.type="button";el.textContent=label;el.addEventListener("click",handler);return el}function renderTeam(data){snapshot=data;const root=$("customer-members");root.replaceChildren();const canManage=Boolean(data.capabilities?.may_manage_members);$("customer-member-create").hidden=!canManage;$("customer-invite-create").hidden=!canManage;$("customer-invitations-section").hidden=!canManage;$("customer-team-summary").textContent=`${(data.members||[]).length} usuário(s) · ${(data.instances||[]).length} instância(s) · função atual: ${data.capabilities?.account_role||"—"}`;for(const item of data.members||[]){const article=document.createElement("article");article.className="server-card team-member-card";const title=document.createElement("h3");title.textContent=item.username;const detail=document.createElement("p");detail.textContent=`Função da conta: ${item.account_role} · ${item.active?"Ativo":"Desativado"}`;article.append(title,detail);if(item.account_role==="owner"){const note=document.createElement("p");note.textContent="Proprietário da conta — não pode ser desativado ou removido.";article.append(note)}else if(canManage){const role=document.createElement("select");role.append(option("member","Member",item.account_role),option("manager","Manager da conta",item.account_role));role.addEventListener("change",()=>mutate("/api/customer/team/members/role",{username:item.username,account_role:role.value}).then(loadAll).catch(show));article.append(role);article.append(button(item.active?"Desativar login":"Reativar login",()=>mutate("/api/customer/team/members/status",{username:item.username,active:!item.active}).then(loadAll).catch(show)));article.append(button("Redefinir senha",async()=>{const password=window.prompt(`Nova senha para ${item.username} (mínimo 8 caracteres):`);if(password===null)return;try{await mutate("/api/customer/team/members/password",{username:item.username,password});$("customer-members-message").textContent="Senha redefinida.";await loadActivity()}catch(error){show(error)}}));article.append(button("Transferir ownership",()=>{if(!window.confirm(`Transferir o ownership da conta para ${item.username}? O Owner atual se tornará Manager.`))return;mutate("/api/customer/team/owner/transfer",{username:item.username}).then(loadAll).catch(show)}));article.append(button("Remover usuário",()=>mutate("/api/customer/team/members/remove",{username:item.username}).then(loadAll).catch(show)))}const accessTitle=document.createElement("h4");accessTitle.textContent="Acesso às instâncias";article.append(accessTitle);for(const instance of data.instances||[]){const row=document.createElement("label");row.textContent=`${instance.name} (${instance.game_id}) `;const select=document.createElement("select");const current=(item.instance_access||{})[instance.id]||"";select.append(option("","Sem acesso",current),option("viewer","Viewer",current),option("operator","Operator",current),option("manager","Manager da instância",current));select.disabled=!canManage;select.addEventListener("change",()=>mutate("/api/customer/team/access",{username:item.username,instance_id:instance.id,permission_profile:select.value}).then(loadAll).catch(show));row.append(select);article.append(row)}root.append(article)}if(!(data.members||[]).length)root.textContent="Nenhum usuário vinculado.";renderInviteAccess()}function renderInviteAccess(){const root=$("customer-invitation-access");if(!root)return;root.replaceChildren();for(const instance of snapshot.instances||[]){const row=document.createElement("label");row.dataset.instanceId=instance.id;row.textContent=`${instance.name} (${instance.game_id}) `;const select=document.createElement("select");select.name=`access:${instance.id}`;select.append(option("","Sem acesso",""),option("viewer","Viewer",""),option("operator","Operator",""),option("manager","Manager da instância",""));row.append(select);root.append(row)}}function renderInvitations(data){const root=$("customer-invitations");root.replaceChildren();for(const item of data.invitations||[]){const card=document.createElement("article");card.className="server-card";const state=item.accepted_at?"Aceito":item.revoked_at?"Revogado":"Pendente";const title=document.createElement("h3");title.textContent=item.email;const info=document.createElement("p");info.textContent=`${state} · função: ${item.account_role}`;card.append(title,info);const grants=document.createElement("p");grants.textContent=Object.entries(item.instance_access||{}).map(([id,profile])=>`${id}: ${profile}`).join(" · ")||"Sem acessos iniciais.";card.append(grants);if(!item.accepted_at&&!item.revoked_at&&snapshot.capabilities?.may_manage_members)card.append(button("Revogar convite",()=>mutate("/api/customer/team/invitations/revoke",{invitation_id:item.id}).then(loadAll).catch(show)));root.append(card)}if(!(data.invitations||[]).length)root.textContent="Nenhum convite registrado."}function renderActivity(data){const root=$("customer-team-activity");root.replaceChildren();for(const item of data.activity||[]){const card=document.createElement("article");card.className="server-card";const title=document.createElement("h3");title.textContent=item.action;const detail=document.createElement("p");detail.textContent=`${item.username||"sistema"} · ${item.result||"—"} · ${item.created_at||""}`;card.append(title,detail);if(item.details){const extra=document.createElement("p");extra.textContent=typeof item.details==="string"?item.details:JSON.stringify(item.details);card.append(extra)}root.append(card)}if(!(data.activity||[]).length)root.textContent="Nenhuma atividade registrada."}async function loadTeam(){renderTeam(await request("/api/customer/team"))}async function loadInvitations(){renderInvitations(await request("/api/customer/team/invitations"))}async function loadActivity(){renderActivity(await request("/api/customer/team/activity"))}async function loadAll(){try{await loadTeam();if(snapshot.capabilities?.may_manage_members)await loadInvitations();await loadActivity()}catch(error){show(error)}}const logout=$("customer-team-logout");if(logout)logout.addEventListener("click",()=>{sessionStorage.removeItem("dsm_auth");sessionStorage.removeItem("dsm_customer_auth");location.href="/customer-login.html"});const memberForm=$("customer-member-form");if(memberForm)memberForm.addEventListener("submit",async event=>{event.preventDefault();const fd=new FormData(memberForm);try{await mutate("/api/customer/team/members/create",{username:String(fd.get("username")||"").trim().toLowerCase(),password:String(fd.get("password")||""),account_role:fd.get("account_role")});memberForm.reset();$("customer-members-message").textContent="Usuário criado e vinculado a este Customer.";await loadAll()}catch(error){show(error)}});const form=$("customer-invite-form");if(form)form.addEventListener("submit",async event=>{event.preventDefault();const fd=new FormData(form),instance_access={};for(const [key,value] of fd.entries()){if(key.startsWith("access:")&&value)instance_access[key.slice(7)]=value}try{const data=await mutate("/api/customer/team/invitations/create",{email:fd.get("email"),account_role:fd.get("account_role"),instance_access});form.reset();renderInviteAccess();$("customer-members-message").textContent=data.delivered?"Convite enviado por e-mail.":"Convite criado. O SMTP ainda não está configurado para entrega automática.";if(data.invitation_token)$("customer-members-message").textContent+=` Token de teste: ${data.invitation_token}`;await loadAll()}catch(error){show(error)}});loadAll()})();
