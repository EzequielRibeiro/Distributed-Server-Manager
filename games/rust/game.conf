#!/usr/bin/env bash

# =============================================================
# Capivara Distributed Server Manager
# Rust process adapter defaults
# Rust Dedicated Server
# =============================================================

GAME_ID="rust"
GAME_NAME="Rust"
GAME_DEFINITION_VERSION="1"
GAME_DEFINITION_TYPE="official"
GAME_SUPPORT_STATUS="supported"
GAME_CATEGORY="survival"
GAME_PLATFORM="linux"
GAME_ARCH="x86_64"
ENGINE="Unity"

# Installation
INSTALL_PROVIDER="steam"
INSTALL_PACKAGE_ID="258550"
INSTALL_AUTH="anonymous"
INSTALL_DIR="/opt/dsm/game-data/rust/serverfiles"
EXECUTABLE="RustDedicated"

# Version model
GAME_VARIANT="stable"
GAME_VERSION="current"
GAME_BUILD="auto"

# Steam
APPID_SERVER="258550"
APPID_WORKSHOP="252490"

# Runtime
PROCESS_ENGINE="native"
PROFILE_PATH="server"
MOD_PATH="oxide"
LOG_EXTENSION=".log"
MOD_PREFIX=""
SAVE_DIR="${INSTALL_DIR}"
LOG_DIR="${INSTALL_DIR}/logs"
CONFIG_FILE="server.cfg"
