# Windows Agent configuration

Runtime configuration is generated during installation under `%ProgramData%\CapivaraAgent\agent.json`.

This source directory intentionally contains no pairing token, permanent credential, machine identity or host-specific setting. Secrets are generated or received only on the target machine.
